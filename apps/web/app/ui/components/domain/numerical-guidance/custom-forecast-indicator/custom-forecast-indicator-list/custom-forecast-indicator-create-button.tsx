import CreateButton from '@/app/ui/components/view/molecule/create-button';

export default function CustomForecastIndicatorCreateButton() {
  return <CreateButton onClick={() => {}} label={'커스텀 예측 지표 추가'} isLoading={false} />;
}
